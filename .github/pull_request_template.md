## Summary

Describe the user-visible outcome and why it is needed.

## Validation

- [ ] `bash scripts/validate.sh`
- [ ] Relevant manual or adversarial checks

## Release labels

Apply exactly one `release:*` label. For an eligible release, also apply
exactly one `change:*` label. `release:none` must not have a `change:*` label.
